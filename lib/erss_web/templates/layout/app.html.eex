<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Erss, take 2</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <%= csrf_meta_tag() %>
  </head>
  <body>
    <nav class="nav justify-content-center nav-justified">
      <%= link "Fics", to: Routes.page_path(@conn, :index), class: "nav-item nav-link" %>
      <%= link "Additional Tags", to: Routes.tag_path(@conn, :index, "additional", 1), class: "nav-item nav-link" %>
      <%= link "Categories", to: Routes.tag_path(@conn, :index, "category", 1), class: "nav-item nav-link" %>
      <%= link "Characters", to: Routes.tag_path(@conn, :index, "character", 1), class: "nav-item nav-link" %>
      <%= link "Fandoms", to: Routes.tag_path(@conn, :index, "fandom", 1), class: "nav-item nav-link" %>
      <%= link "Relationships", to: Routes.tag_path(@conn, :index, "relationship", 1), class: "nav-item nav-link" %>
      <%= link "Warnings", to: Routes.tag_path(@conn, :index, "warning", 1), class: "nav-item nav-link" %>
    </nav>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= render @view_module, @view_template, assigns %>
    </main>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
